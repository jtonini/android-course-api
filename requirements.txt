# Core dependencies
Flask==3.0.0
Werkzeug==3.0.1
gunicorn==21.2.0

# Configuration support (if using config.toml)
tomli==2.0.1; python_version < '3.11'

# Optional but useful for production
python-dotenv==1.0.0  # For environment variables (alternative to config.toml)
